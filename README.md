# echomail_frontend

React + Vite + TanStack Router frontend for the EchoMail email marketing platform.

## Dev

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Set `VITE_API_URL` to the backend base URL (e.g. `https://your-domain.com/echomail/api/v1`) before building.
